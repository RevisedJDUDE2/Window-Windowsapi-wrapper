# The Window Context creator wrapped in windows api...
# how does it **work?**

first **do**: <br>
on your terminal type
`cd build` then `make`
then you can link the .a file as an library and the dll aswell

second **if you want to**: <br>
you can **run it by doing `Autoclicker.exe`**
